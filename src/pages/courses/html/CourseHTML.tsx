import React from 'react';
import Layout from '../../../components/layout/Layout';
import Typography from '../../../components/typography/Typography';

const CourseHTML = () => {
  return (
    <Layout>
      <Typography variant="heading">HTML</Typography>
    </Layout>
  );
};

export default CourseHTML;
